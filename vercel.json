{
    "version": 2,
    "builds": [
        {"src": "app.py", "use": "@vercel/python"}
    ],
    "functions": {
        "api/**/*.py": {
            "maxDuration": 60 // Timeout in seconds
        }
    },
    "routes": [
        {"src": "/(.*)", "dest": "app.py"}
    ]
}
